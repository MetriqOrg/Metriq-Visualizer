Drop your .mvpreset files into this folder.

Metriq Visualizer will list .mvpreset files from this presets/ directory in the Visualizer Behavior preset dropdown.
Only the visual/behavior settings inside each preset are applied from that dropdown.
