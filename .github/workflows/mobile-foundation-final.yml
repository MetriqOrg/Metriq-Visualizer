name: Mobile GPU foundation final

on:
  push:
    branches:
      - codex/visualizer-mobile-gpu-foundation-final
  pull_request:
    paths:
      - 'mobile/**'
      - 'docs/MOBILE_GPU_FOUNDATION.md'
      - '.github/workflows/mobile-foundation*.yml'

permissions:
  contents: read

jobs:
  validate-mobile-source:
    uses: ./.github/workflows/mobile-foundation.yml
